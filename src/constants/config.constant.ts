export enum ConfigEnum {
  MYSQL = 'mysql',
  REDIS = 'redis',
  BULLMQ = 'bullmq',
  APP = 'app',
  MONGO = 'mongo',
  WINSTON = 'winston',
  RATELIMIT = 'ratelimit',
  CACHE = 'cache',
  /**microservice based on tcp */
  MICRO_TCP = 'micro_tcp',
}
